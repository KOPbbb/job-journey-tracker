# 求职轨迹

这是一款只在本机运行的求职投递管理工具。记录保存在浏览器本地，不会自动上传到网络。

## 日常启动

双击 `启动求职轨迹.command`。软件会自动在浏览器中打开；关闭启动窗口即可停止运行。

如果 macOS 第一次阻止打开，请右键点击该文件，选择“打开”，再确认一次。

## 可以做什么

- 新增、编辑、删除投递记录
- 上传和管理 PDF、DOC、DOCX 简历版本
- 为每条投递选择当时使用的简历版本
- 按企业、岗位和状态搜索筛选
- 使用列表、每日安排日历或看板查看求职进度
- 按日期集中查看投递、笔试、面试和跟进提醒；在某一天新增投递会自动填入该日期
- 查看近期面试、笔试和跟进安排
- 查看投递漏斗和渠道统计
- 导出 CSV 表格或 JSON 完整备份
- 从 CSV、JSON 恢复数据

## 数据备份

建议在重要更新后进入“数据备份”，下载一份 JSON。JSON 会保留投递字段和简历版本关联，CSV 更适合用 Excel、Numbers 或 WPS 查看。简历文件本身请在“简历版本”页面分别下载保存。

GitHub 用于管理软件代码的版本，不会自动同步这台电脑中的投递记录或简历文件。

## 开发命令

```bash
npm run dev
npm run build
npm run lint
```
